{
  "name": "mr-story-night",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Zero-prep narrative roleplaying system for Foundry VTT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "release": "npm run validate && npm test && npm run build"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
