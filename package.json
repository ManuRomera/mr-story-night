{
  "name": "mr-story-night",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "GM-less table for playing Follow in Foundry VTT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "release": "npm run validate && npm test && npm run build"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
